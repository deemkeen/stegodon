conf:
  host: 127.0.0.1 # local server ip
  sshPort: 23232 # ssh login port
  httpPort: 9999 # local http port
  sslDomain: example.com  # your hosted ssl domain (use ngrok domain for local testing)
  withAp: false # activitypub (experimental!)
  single: false # single-user mode (only one user can register)
  closed: false # closed registration (no new users can register)
  maxChars: 150 # maximum characters allowed in a note (can be overridden by STEGODON_MAX_CHARS env var, maximum 300)
  showGlobal: false # show global timeline (local + federated posts, can be overridden by STEGODON_SHOW_GLOBAL env var)
  showTos: false # show terms of service acceptance screen (can be overridden by STEGODON_SHOW_TOS env var)

# For local federation testing:
# 1. Run: ./test-federation.sh
# 2. Or manually:
#    - Start ngrok: ngrok http 9999
#    - Run with: STEGODON_WITH_AP=true STEGODON_SSLDOMAIN=your-ngrok-domain.ngrok-free.app ./stegodon
# See TESTING.md for complete instructions
